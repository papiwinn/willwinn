# Correction inbox (JSON)

One file per submission, named `<github-issue-number>.json`.

Created by the `archive-correction` GitHub Action when an issue labeled `correction` is opened. Safe to edit or delete after you handle a correction.
